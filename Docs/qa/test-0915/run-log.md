# TEST-0915 — run log

Named testing session on the `feat/motion-0914` stack tip. Appended after every
step: timestamp (UTC), command, result. This file is the record if the session
dies. Commit SHAs only, never deployment ids.

Session start: **2026-09-15T07:01:31Z** (first command of the session; the
ledger reads before it began ~06:55Z).

## Phase 0 — pre-flight

| Time (UTC) | Step | Command / check | Result |
| --- | --- | --- | --- |
| 06:58Z | 0.1 | `git fetch origin`; `git rev-parse origin/{main,live,feat/ux-0913,feat/theme-0913,feat/demo-0914,feat/motion-0914}` | **MATCH.** origin/main `0482f20` · origin/live `0482f20` · ux `bb83ffe` · theme `6fb7bcd` · demo `9e81545` · motion `9d4fdbc`. HEAD = `feat/motion-0914` @ `9d4fdbc`, working tree clean. Local `live` is a stale ancestor (`590172c`), fast-forwarded in Phase 5 only. |
| 06:58Z | 0.2 | `netstat -ano \| grep -w 5199`; `Get-CimInstance Win32_Process` matched on `vite preview\|playwright test\|gate.ts\|keep-awake\|ms-playwright` | **CLEAR.** Nothing on 5199; no Vite, Playwright or keep-awake process alive (only the session's own shells matched their own command line). Nothing to kill. |
| 06:59Z | 0.3 | `.env.local` present, gitignored (`.gitignore:17`); `VITE_API_BASE_URL` = the eu-west-2 Lambda URL | **DEV API.** `Docs/api/environments.md` names "the Lambda URL in `.env.local`" as the only dev API today; no production base exists yet (item 53). Only `VITE_API_BASE_URL` is in the file; the funded creds live in the User-scope registry (stack.md), and `E2E_API_ENV=dev` is exported per run. |
| 07:00Z | 0.4 | Read `.agent/state.md`, `.agent/open-items.md` (62–72), `.agent/decisions.md` (2026-09-13 ruling, D-INT-B amended, D-UX-0913-C/D, D-THEME-0913-A…K, D-DEMO-0914-A…D, D-MOTION-0914-A…L), `.agent/sessions.md` (GATE-0910 §4 close, both VOICE-0913 entries, UX/THEME/DEMO/MOTION entries), `playwright.config.ts`, `e2e/live-setup.ts`, `scripts/gate/{gate,classify}.ts`, `e2e/global-setup.ts` | Confirmed from the repo: **the three brand-voice fixes** (VOICE-0913, decisions.md "D-INT-B AMENDED", item 65) are (1) the adapter reads the CANONICAL row only, resolved in CREATION order (`brand-adapter.ts:102-144`), extra rows surfaced as `extraVoiceRows` with the notice `MESSAGES.notices.brandVoiceExtraRows` (`messages.ts:394`, "…Ask a workspace owner or admin to merge them into this list."); (2) `MAX_BRAND_VOICE_RULES = 40` combined across Do and Don't (`types.ts:546`), below the wire's **50** (`Docs/api/api.md:675`), Add disabled at the cap, a list above it still saveable when it shrinks (`brand.ts:205-235`); (3) a refused save renders the wire's field message + request id in a `role="alert"` and `toastSuccess` fires only after a save that succeeded (`brand-voice-screen.tsx:183-206`). **The two GATE-0910 reporting fixes** (sessions.md 2026-09-13 "§4 accepted", `classify.ts`): `skipLabel` → "not run, an earlier test in this file failed" for a reason-less skip in a file that failed, counted in the report's own "Not run" column (`gate.ts:646-668`); and `isRerunnable` widened from `network-lost` to `error-page` (`APP_ERROR_PAGE` matched against the DOM kept at the failure, never the assertion text). |
| 07:01Z | 0.5 | `mkdir Docs/qa/test-0915`; this file | Created. |
| 07:02Z | 0.6 | lockfile check: `diff pnpm-lock.yaml node_modules/.pnpm/lock.yaml` | **UNCHANGED** — no `pnpm install`. Baseline chain (lint → typecheck → guard-static → unit → build) launched in the background; result below. |
| 07:03Z | 0.6 | `pnpm lint` · `pnpm typecheck` · `pnpm guard:static` · `pnpm test` · `pnpm build` (sequential, logs in the session scratchpad `p0/`) | **BASELINE MATCHES.** lint clean (8 s) · typecheck clean (18 s) · guard-static **373 files clean** · unit **808 passed / 0 failed in 69 files** (26 s) · build OK (11 s vite, entry `index-BA8lPKYr.js`). No drift. |

## Phase 1 — known items, before the gate

| Time (UTC) | Item | Command | Result |
| --- | --- | --- | --- |
| 07:06Z–07:08Z | **71** | `pnpm gate --skip-static --only live-brand-kit --rounds 1 --lanes A --workers 1 --series test-0915` (the runner: live build with the dev base, `vite preview` on 5199 owned by pid, fleet warm in 1.0 s, one file, `--workers=1`) | **GREEN — 3 passed / 0 skipped / 0 failed in 33 s.** Both never-run edits executed: line 116 (the wire test) and line 157 (the browser-truth test) assert `[data-slot="file-role-badge"]` → `Brand kit`, and the wire echoed a role the table maps. Record `Docs/qa/test-0915/gate/20260915-070659/` (tree `9d4fdbc`). **Item 71 closes.** |
| 07:10Z | **64** | `node mint-org.mjs i64` (API only, zero spend): org **2170**, voice 362 = `[dont: Call anything artisanal]` (requests `f79948b6…`, `4d947e6f…`, `f668c926…`, `299aac56…`, `16cfadc1…`) | Throwaway org minted with ONE Don't row and no Do row. |
| 07:12Z | **64** | `playwright test e2e/item-64-proof.spec.ts --workers=1` (live dev server, `ITEM64_SELECTOR=old` — the write site byte-identical to `live-brand.spec.ts:111` / `live-setup.ts:173,396`) | **DEFECT PROVEN — RED.** Before "Add do", `input[id^="voice-do"]` matched `["voice-dont-0"]`. After "Add do" + `.last().fill(…)`: on screen `Do rule 1 = ""`, `Don't rule 1 = "Name the farm when it matters"` (the stored Don't rule overwritten). After Save the wire held `["dont: Name the farm when it matters"]` and no do rule (GET voices request `19beee5f-02b2-48b0-b191-bfcab36c2f1d`). Screenshot `Docs/qa/test-0915/item-64/item-64-old-before-save.png`. Log `item-64-old.log` (record folder). |
| 07:14Z–07:18Z | **64** | `node mint-org.mjs i64b` → org **2171** (voice 363, `[dont: Call anything artisanal]`; requests `378aa153…`, `d4e60d8e…`, `495054cc…`, `fe1ffd85…`, `2c9e77ea…`). Fix applied: one helper `addVoiceRule(page, 'Do' \| "Don't", text)` in `e2e/live-setup.ts` targeting the new row by the exact label `RuleList` writes (row count read BEFORE Add; no `.last()`); six write sites and four read sites in `live-brand-rules`, `live-brand`, `live-onboarding`, `live-setup` (`ensureFundedBrand`, `completeBrandSetup`) moved to exact labels. `prettier --write` on the four (all clean at HEAD; only the edited lines changed) · lint · typecheck clean. Then `playwright test e2e/item-64-proof.spec.ts --workers=1` with `ITEM64_SELECTOR=new` on org 2171 | **FIX PROVEN — GREEN.** Before Add the prefix selector still matched only `voice-dont-0` (the trap is real on this org too); with the helper: on screen `Do rule 1 = "Name the farm when it matters"`, `Don't rule 1 = "Call anything artisanal"`; wire after Save `["do: Name the farm when it matters", "dont: Call anything artisanal"]` (request `b21a362f-715e-4229-9291-905834df41d4`). Screenshot `item-64/item-64-new-before-save.png`, log `item-64/item-64-new.log`. **Commit `454e98e`** (`TEST-0915: item 64 …`). No product code changed; the screen and its PATCH were always correct. **Item 64 closes.** |
| 07:18Z | **70** | rule for the session | Every Playwright run in this session passes `--workers=1` on the command line (the runner's live files always do; the static suite is run as `pnpm exec playwright test --workers=1` rather than through `verify:all`, whose e2e step has no worker control — see the Phase 2 note). `playwright.config.ts` untouched. |
| 07:18Z | **63** | classification rule for the session | `live-video-duration`'s over-maximum `durationS` red is **KNOWN** (Hasan; the maximum is checked after the wallet, requests `a3cb0dca…`/`25c4c86c…` on record). Not chased, not skipped, not softened. |

## Phase 2 — the full gate on the tip (`454e98e`)

| Time (UTC) | Stage | Command | Result |
| --- | --- | --- | --- |
| 07:20Z | launch | `bash gate1.sh gate1` (detached): lint → typecheck → guard-static → unit → build → `playwright test --workers=1 --reporter=list,json` (static, `VITE_API_BASE_URL=''`) → `pnpm gate --skip-static --workers 1 --rounds 1 --series test-0915` (the 21 live files, one at a time, on the built app). **Note:** `verify:all`'s e2e step runs Playwright at the default worker count with no flag to change it, so the static suite is run directly with `--workers=1` (the session rule, item 70) and the seven `verify:wNN` report checks are not part of this gate's record. | running |
| 07:20Z–07:28Z | attempt 1 on `454e98e` | `gate1.sh` | lint 8 s · typecheck 17 s · guard-static **373 clean** · unit **808/808 in 69 files** · build OK · **static e2e `--workers=1`: 115 passed / 0 failed / 85 skipped in 350 s** (the 85 are the live files' own "live-mode run only" skips). The live half **STOPPED at the runner's own build step** (record `gate/20260915-072746`, STOPPED): `tsc -b` refused the tree because the two Phase 3 proof scripts I had just written under `scripts/` carried a type error (a local `login` shadowing the `login` helper). Not a product finding — the session's own tooling. Fixed, formatted, committed as **`3576305`** (`TEST-0915: the Phase 3 proof scripts …`); lint · typecheck · prettier clean. |
| 07:31Z | attempt 2 on `3576305` | `gate1.sh gate1` relaunched in full (static chain + static e2e + the 21 live files) | running |
| 07:54Z | classification | the record `Docs/qa/test-0915/gate/20260915-073808/` (RED): 19 of 21 files green or all-skipped; two reds, classified before any fix | **`live-video-duration` › the over-maximum body → bucket c KNOWN (item 63).** `durationS:999` answered 402 where the contract says 400; reproduced today at zero spend on throwaway org 2197 (wallet 0 → 0, no job): `"abc"` → 400 `bad_request` request `2f6229da-b800-497a-b92e-06877bda2755`, `999` → 402 `wallet_insufficient` request `81cf254c-2cec-441d-bf7b-c7b3492caf91` (wallet reads `abebbb5d…`/`0b9d4966…`, jobs `e560cf9f…`). Spec untouched. **`live-scheduling` › C1 creates the schedule … survives a reload → bucket b REAL REGRESSION.** The DOM at failure shows the SaveBar in its `role=status` "Saved" state with no Save button while the form is dirty (posts per day 2 after the click). Cause, read in `src/components/ab/save-bar.tsx` (commit `b7c338e`, THEME-0913 §5.3): "Saved" is inferred from `dirty` going true → false and held 1.6 s; the effect's cleanup clears that timer whenever `dirty` changes again, so an edit inside the window leaves the bar stuck on "Saved" with its buttons hidden; and a reload's sync produces the same transient dirty edge, so the bar says "Saved" when nobody saved. No decision licenses either (D-THEME-0913-K licenses feedback at the control, not a stuck bar or a false claim). Fix follows, unit test written first. |
| 07:58Z–08:02Z | fix (bucket b) | `src/components/ab/save-bar.test.tsx` written FIRST (four tests) and run on the unfixed bar: **3 of 4 red** — the edit-inside-the-window stuck state, the sync-flicker false "Saved", and Cancel-after-a-failed-save saying "Saved" (`Docs/qa/test-0915/save-bar/unit-before-fix.log`). Fix in `save-bar.tsx`: the clean edge counts only after THIS bar's Save was pressed (`savePressed` ref, reset by Cancel and consumed by the edge), and `dirty` going true clears "Saved" at once with the buttons back. **4/4 green after** (`unit-after-fix.log`). §5.3's licensed feedback unchanged (test 1). prettier · lint · typecheck clean. | commit below; live proof next |
| 08:02Z | fix proven live | **commit `f2b1adc`** (`TEST-0915: bucket b - the SaveBar's "Saved" …`), then `pnpm gate --skip-static --only live-scheduling --rounds 1 --lanes A --workers 1 --series test-0915` on the built app | **GREEN — 2 passed / 1 skipped (its own reason: no slots on a fresh org, item 60) / 0 failed in 28 s.** Record `Docs/qa/test-0915/gate/20260915-080131/`. The same test that failed in gate 1 passes on the fixed bar. |

## Phase 3 — targeted proofs

| Time (UTC) | Proof | Command / evidence | Result |
| --- | --- | --- | --- |
| 08:03Z–08:09Z | A, B, C, F (live), I — run 1 | `run-proofs-live.sh`: live build (entry `index-Ba0MfkxF.js`, API host inlined 1×) served by `vite preview` on 5199; `scripts/probe-test-0915-live.ts` on orgs **2199** (A/C/I; voices 371 canonical + 372 extra, both "Brand voice") and **2200** (B; 45 rules); a fresh org **2201** minted through the product for F. Record `Docs/qa/test-0915/proofs/proofs-live.md` (run 1) + screenshots | **A — PASS on all 7 checks:** the wire lists `[372, 371]` (createdAt DESC, request `19d0f5f2…`), the screen reads the OLDEST row 371 (creation order): `Do rule 1 = "Canonical do rule"`, `Don't rule 1 = "Canonical dont rule"`, no "Extra…" input; the notice renders verbatim "…Ask a workspace owner or admin to merge them into this list. (1 extra row, 2 rules)"; counter `2 / 40`; removing Do rule 1 through the screen shrank ONLY row 371 → `[dont: Canonical dont rule]`, row 372 untouched (request `fa9f8c56…`); a reload still shows the canonical row only. **C — the honesty half PASSES, and a finding:** the intercepted PATCH 400 produced NO "Brand voice saved" and an error toast carrying the wire's own field message; but the in-screen `role=alert` (with the request id) and the user's draft were WIPED within a second — `saveBrandVoice`'s failure path calls `resync()` (INT-3, 2026-07-30, `brand.ts`), the Settings layout renders its skeleton while `liveSyncPhase` is `syncing`, and the outlet remounts pristine. Pre-existing on `main`, not this stack's; filed as a new open item, not a blocker. **B — my locator was ambiguous** (`/across Do and Don/` matched the counter and the cap sentence): re-run below. **F — the seed boundary holds on the wire** (fresh org 2201: voices 0 `efad443f…`, sources 0 `234fc365…`, topics 0 `33aee5d8…`, proposals 0 `509f7862…`; 0 Approve buttons) — my Today sample was taken while the queue was still "Reading your queue…": re-run below with a real wait. **F — the Connect flow DOES show a success state** in LIVE: `?connect=success&platform=facebook` → "Connecting your Facebook account… Almost there. We are storing the permissions you granted." → toast "Facebook connected / Posting and analytics are on." → the Facebook card badge "Active" — a connection the product cannot make in either mode: **bucket b by the order's honesty rule**, fix below. F's LIVE Analytics check ran AFTER that fake flip (one "connected" channel → the populated screen), so it is a consequence, not a second defect; re-ordered in the re-run. **I — variant 2 PASS** (an expired token at boot: purge, toast "Your session ended. Sign in again to continue.", `/login`, storage empty; api.md:21 — no refresh endpoint); variant 1 (revoked, request `b3f62f14…` → 204) met the 401 on the Settings tab READ and landed on login BEFORE my script's write step — the product did the right thing earlier than the script expected: re-run below with the read as the trigger. First light did NOT appear in LIVE mode on the fresh account (NOTE) — investigated below. |
| 08:11Z–08:22Z | A–I live, run 2 (all five, on the build with the Connect fix `7af4be5`; the run-2 script edits) | record `proofs/proofs-live-run2.md` (run 1 kept as `proofs-live-run1.md`) | **B — PASS in substance:** above the cap the counter reads `45 / 40`, the cap sentence is on screen, both Add controls disabled, all 30 + 15 rows listed; removing one and saving landed **44 on the wire** (request `df14b6ee…`), Add still disabled; the wire itself refuses 51 rules (400 `validation_failed`, `525504b0…`) and accepts 50 (201, `39713604…`; deleted, 204 `cf2e9625…`) — the contract's 50 stands and the product stops at 40. (My counter read right after the click caught the MonoNumber tween mid-flight, D-MOTION-0914-E; re-read after 400 ms in run 3.) **C — PASS on the honesty half, FAIL on the alert half** (the finding, filed as item 73): never "saved", the error toast carries the wire's field message, the wire unchanged (`5293b483…`); the in-screen `role=alert` never survived the failure path's resync. **F — PASS:** fresh org 2202 — Today's empty state "Nothing here", 0 cards, voices 0 (`fe9ffa38…`), sources 0 (`3eb0df2f…`), topics 0 (`833aee01…`), proposals 0 (`f9b70b6b…`); LIVE Analytics "Analytics arrive with publishing" with no control; LIVE Connections notice present, all four badges "Not connected"; **the Connect flow on the fixed build: "Facebook is not linked yet …", no toast, badges unchanged — PASS.** **First light in LIVE: NEVER MOUNTED although the account flag was written** (in-page observer) → bucket b, the stack's own moment: the gate re-read the flag the overlay writes, and the workspace sync's re-render dismissed it. Fixed (commit below). **I — variant 2 PASS**; variant 1 met the 401 on the Settings tab list before my script's tab click (the product landed on login first; script fixed, run 3). |
| 08:23Z–08:26Z | fix (bucket b) | `src/components/ab/first-light-gate.test.tsx` written FIRST: the mid-moment case red on the unfixed gate (`Docs/qa/test-0915/first-light/gate-unit-before-fix.log`); fix in `first-light-gate.tsx` (the decision is taken once per arming and kept through re-renders); **14/14** with the existing first-light tests after; lint · typecheck clean | **commit `b66b156`**. Proven live in run 3 below. |
| 08:25Z–08:31Z | B, F, I — run 3 (on the build with `b66b156` + `7af4be5`) | record `proofs/proofs-live-run3.md` | **B — PASS on all three checks:** above the cap (44 stored) the counter reads `44 / 40`, the cap sentence is on screen, both Add controls disabled, 29 + 15 rows listed; removing one and saving → counter `43 / 40` (read after the tween) and **43 on the wire** (`86a19411…`), Add still disabled; the wire refuses 51 (400 `ed1a6564…`) and accepts 50 (201 `be016822…`; deleted 204 `4a604ebb…`). **F — PASS on all four:** fresh org 2203 — Today "Nothing here", 0 cards, voices/sources/topics/proposals all 0 (`2bc9f075…`, `3f4c19fc…`, `bff24643…`, `bb6a8e04…`); Analytics "Analytics arrive with publishing", no control; Connections notice, all badges "Not connected"; Connect → "Facebook is not linked yet …", no toast, badges unchanged. **First light in LIVE on the fixed gate: VISIBLE to the harness on the fresh account** (flag written; the fix holds). **I — variant 2 PASS**; variant 1 (revoked mid-session on an authed route) ends on `/` with the session purged and no toast left after 40 s: sampled at 200 ms in run 4 below before classifying. |
| 08:33Z–08:44Z | I — runs 4–6 | the revoked-token walk instrumented (run 6 samples url/toast/session every 200 ms from the revocation; request `3c6f25d3…`) | **I — boot path PASS (four runs)**: an expired token at reload → 401 on the first sync → session purged, toast, `/login`. **Mid-session path: FAIL, filed as item 75** — the handler fires (toast and purge at **413 ms**) but the URL runs `/ → /settings → /`: the authed route renders the cleared session first and its `<Navigate to="/">` supersedes the handler's push to `/login`. Pre-existing on `main` (INT-era code, untouched by the stack); not a gate red; a routing ruling is needed, so not fixed here. |
| 08:28Z–08:41Z | E, F (static), G — static runs 1 and 2 | `run-proofs-static.sh` (static dev server, mode confirmed empty base) → `scripts/probe-test-0915-static.ts`, the navigation probe (5 runs/route), the screenshot set; then `--proofs firstlight,nav` re-run with the in-page observers fixed (they observed `documentElement`, which is null when an init script runs). Records `proofs/proofs-static.md`, `proofs/proofs-static-run2.md`, `proofs/nav/`, `screens/` | **E — first light:** plays on a fresh account, axe **0** mid-animation; **1806 ms** on screen (ceiling 2000); the account flag written **11 ms** after mount and read `"1"` at removal; a welcomed account is never welcomed again; a reload never replays; a storage that throws → not shown, the app still lands; reduced motion → not mounted, same landing. **E — moments:** 1 (tone sample) axe 0 at rest and mid-rewrite; 4 (generating) "Writing your draft…", no percentage, axe 0 mid-stage; 3 (approve) click → settled median **41 ms** (n=2 of 3 clicks measured), axe **1 serious `color-contrast` node at +80 ms — the toast description while sonner's own 400 ms entrance fade is in flight** (filed as item 76); with the toast UP: **0 violations, 13.62:1** (`rgb(232,232,232)` on `rgb(22,31,38)`). **E — every app screen inside its 220 ms entrance: 22 routes, 0 violations each.** **E — skeleton:** `role=status`+`aria-busy` at mount, first paint **231 ms** after mount (4 ms polling). **E — reduced motion:** fast/medium/slow → 0ms, button transitions 0s, `--skeleton-delay` stays 220ms; `data-ab-motion` elements 2/2 hidden, none is or contains a control, Today's controls **40 vs 40**; the approve settle at its end state at once (48 ms, animation 0s, opacity 1); no skeleton flash on five routes. **E — route timings (medians, 5 runs):** Today **70** · Studio **56** · Billing **66** · Calendar **53** · Settings **90** ms, skeleton visible 0/5 everywhere. **G — press:** Approve at rest `rgb(255,78,45)`, pressed `rgb(232,65,34)` = `--primary-pressed #e84122`, label `rgb(26,10,5)` at **4.78:1**; `--c-accent-lo` absent from `:root`. **G — indicator:** ONE element per rail (count 1); the settings sub-nav's SLIDES between tabs (same node, 14 intermediate transforms); the rail's does NOT slide between screens — a new node at **77 ms**, `data-placed=false` then a fade, only two transforms ever seen — because every screen renders its own `AppShell` (filed as item 77; D-MOTION-0914-C's between-route claim narrowed). **F (static):** Connections notice present, all badges "Not connected"; Connect → "Facebook is not linked yet…", no toast; Analytics title with no control. **Guards and the fifth-moment test next.** |
| 08:41Z | E — the guards bite; F — the nine seed tests | `run-guards.sh` (`Docs/qa/test-0915/guards.log`) | `first-run-seed.test.ts` **9/9** (nothing publishes, bills or reaches a paid path). `motion-scale.test.ts` control **20/20**; **break 1** a fifth `data-ab-motion` family in the moments block → "there are FOUR, and the vocabulary is the beacon" **failed**, reverted; **break 2** `--motion-fast: 120ms` in the reduced block → "zeroes EVERY duration in the scale" **failed** naming the token, reverted; **break 3** `FIRST_LIGHT_MS = 2_100` → "is under the two-second ceiling" **failed** (2100 < 2000), reverted. Controls green again on the restored tree; `git status src/` clean. |
| 08:42Z–08:44Z | D1 — the skip label and the Not-run column | `run-proof-d.sh`: `live-brand-kit.spec.ts` test 1 broken deliberately (an absent heading, 1 s timeout), `pnpm gate --skip-static --only live-brand-kit --rounds 1 --lanes A --workers 1 --series test-0915`, then `git checkout` | **PROVEN.** Record `gate/20260915-084209`: the row reads Passed 0 · Skipped **0** · **Not run 2** · Failed 1, and both cascaded tests carry "_not run, an earlier test in this file failed_"; the red itself is UNCLASSIFIED (a product-shaped assertion, not the service's). Spec reverted, 0 changes left. |
| 08:44Z–08:46Z | D2 — first attempt | the same file, test 2 breaking ONCE behind a 500 on the org sync's voices read (marker consumed on the first run) | **Did not exercise the rule**: the glob `**/brand/voices` missed the read's query string, so nothing broke and the file was green first time (record `gate/20260915-084249`). Redone with a URL predicate below. |
| 08:44Z–08:48Z | D2 — redo (a URL predicate matching the read's query string) | `run-proof-d2.sh`: test 2 fails ONCE behind the app's own error page (500 on the org sync's voices read, marker consumed on the first run), then `git checkout` | **The widened rule PROVEN, and a verdict gap found.** Record `gate/20260915-084446`: the first run 1 passed / 1 **not run** ("_not run, an earlier test in this file failed_") / 1 failed → classified **error-page** from the DOM at failure (the app's own "We couldn't load this screen"), re-run solo **3/3 green** (30 s, 28 s, 30 s). But the README's verdict is **RED**: `gate.ts`'s `gateGreen` forgives only `network-lost`; `error-page` was added to `isRerunnable` (the re-run) but not to the verdict. A harness defect (GATE-0910's second reporting fix half-wired); fix with a pure `gateRoundGreen` in `classify.ts` plus a unit test, applied AFTER gate 2 so the runner is not edited under its own run. |

## Phase 4 — the second gate

| Time (UTC) | Stage | Command | Result |
| --- | --- | --- | --- |
| 08:46Z–08:55Z | gate 2 static half on `fb5c33b` | `gate1.sh gate2` | lint · typecheck · guard-static 373 · **unit 815/815 in 71 files** (+4 SaveBar, +3 first-light gate) · build · **static e2e 115 passed / 1 failed / 85 skipped in 351 s** (116 tests now: the Connect walk joined). The red: `compose-analytics-settings.spec.ts:238` "with no channel left to report…" — **bucket a, SPEC TO UPDATE (D-TEST-0915-C):** it reconnected LinkedIn through B3's fake `success` return only to be able to Disconnect it and reach a channel-less analytics screen; the return links nothing now, and a channel that needs re-auth offers no Disconnect, so the demo world can no longer be emptied through the product. The spec now reaches the same state the way an org actually starts — a fresh signup into the seeded review world, which has no connection (D-DEMO-0914-B) — and asserts nothing is asked of the person. Run alone after gate 2's live half; a third full gate follows on the final tip. |
| 08:54Z–09:10Z | gate 2 live half on `fb5c33b` | `pnpm gate --skip-static --workers 1 --rounds 1 --series test-0915` → record `gate/20260915-085449` | **19 of 21 green or all-skipped; `live-scheduling` GREEN (2 passed / 1 own-reason skip) — the SaveBar fix holds under the runner.** Two reds, both **bucket c**: `live-video-duration` (item 63, as in gate 1); and **`live-media-capabilities` › "every granted capability…" — `photoshoot.generate` with five `referenceImages` answered 400 `bad_request` (request `abc39336-4b76-4999-9edd-996da3dc516d`) where the spec asserts the 502 measured on 2026-09-10 (item 58). Gate 1 got the 502 at 07:44Z today; the wire changed under the session — the validation item 58 asked Hasan for appears to have landed. Root cause is item 58: KNOWN, spec left as is, for the founder to confirm with Hasan before the assertion moves to 400.** Runner verdict RED (the two unclassified reds are the session's bucket c). |
| 09:10Z–09:12Z | bucket a commit; the runner verdict fix (harness) | `7e776b6` the analytics walk (23/23 alone); `299aff6` extracts the runner's old verdict whitelist into `gateRoundGreen` and adds the test — **red on the extracted whitelist** (`runner-verdict/unit-before-fix.log`); `d793cb1` widens it through `isRerunnable` and wires `gate.ts` — **20/20 after** (`unit-after-fix.log`); lint · typecheck clean | D2 on the OLD runner once more (`gate/20260915-091252`): error-page, re-run 3/3, verdict still RED — the defect reproduced; D2 on the fixed runner below. |
| 09:15Z–09:18Z | D2 on the FIXED runner (`d793cb1`) | `run-proof-d2.sh` once more → record `gate/20260915-091544` | **GREEN.** The same first-run red (error-page from the app's own "We couldn't load this screen"), the same 1 not-run with its label, re-run solo 3/3 (28 s, 27 s, 27 s) — and the verdict now reads "Every red in the gate round was classified network-lost or error-page and re-run green 3/3, or there was none." Proof D closes on both reporting fixes, with the verdict gap found and fixed. Spec reverted, 0 changes left. |

## Phase 4 (continued) — the third full gate, on the code tip

| Time (UTC) | Stage | Command | Result |
| --- | --- | --- | --- |
| 09:18Z | launch | `gate1.sh gate3` on **`d793cb1`** (every code, spec and harness fix committed; only the docs are uncommitted — the runner records the tree as dirty for that reason) | running |
| 09:18Z–09:41Z | gate 3 on `d793cb1` | `gate1.sh gate3` → static half + `pnpm gate --skip-static --workers 1 --rounds 1 --series test-0915` (record `gate/20260915-092532`) | **CLEAN EXCEPT BUCKET c.** lint 7 s · typecheck 16 s · guard-static **373 clean** · unit **817 passed / 0 failed in 71 files** · build OK · **static e2e 116 passed / 0 failed / 85 skipped in 340 s** (one worker) · live **20 of 21 files green or all-skipped** (`live-create-visual` all-skipped by its own `LIVE_MEDIA` reason; `live-proposals`/`live-studio`/`live-scheduling` skips carry their own reasons), the one red **`live-video-duration` = item 63, bucket c**. `live-media-capabilities` GREEN again (3/3 in 80 s): the five-reference body answered **502** at 09:31Z after 400 at 09:00Z — item 58's wire answer FLAPS (annotated on the item), not fixed. Runner verdict RED on the one bucket-c red, as expected. **Merge authorised.** |
